package GameObjects.Cards.Spells;

public class Sigil extends Spell{

    //TODO It is actually a normal spell 'QuestionMark'?
    public Sigil(int x, int y, int width, int height, String file, int manaCost, CardClass classCard) {
        super(x, y, width, height, file, manaCost, classCard);
    }
}
